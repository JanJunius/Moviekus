<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Moviekus.Views.Movies.MovieDetailPage"
             Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Löschen" Command="{Binding DeleteCommand}" IconImageSource="delete.png" />
        <ToolbarItem Text="Ändern" Command="{Binding EditCommand}" IconImageSource="edit.png" />
    </ContentPage.ToolbarItems>

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Titel:" FontSize="Medium" />
        <Label Text="{Binding Movie.Title}" d:Text="Name des Films" FontSize="Small"/>
        <Label Text="Genres" />
        <Label Text="{Binding Genres}" FontSize="Medium"/>
        <Label Text="Verfügbar bei" />
        <Label Text="{Binding Movie.Source.Name}" FontSize="Medium"/>

    </StackLayout>
    
</ContentPage>